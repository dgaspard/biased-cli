services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    volumes:
      - ./src:/app/src
      - ./biased:/app/biased
    environment:
      - PROJECT_NAME={{PROJECT_NAME}}
    restart: unless-stopped
